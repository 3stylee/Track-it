import styled from "@emotion/styled"
import { TITLE_BANNER_HEIGHT } from "../../../../constants"

export const CentralContainer = styled("div")`
	display: flex;
	justify-content: center;
	margin-top: calc(7rem + ${TITLE_BANNER_HEIGHT});
`
